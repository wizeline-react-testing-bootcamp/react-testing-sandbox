describe("Jest Sandbox", () => {
  test.todo(
    "Test that severalReturnTypesFunction is for certain value is matching the FullObject interface"
  );

  test.todo(
    "Test that severalReturnTypesFunction is returning null for certain values"
  );

  test.todo(
    "For severalReturnTypesFunction(4), what is expected? string? float? number?, find out and test return value and it's type."
  );

  //   https://jestjs.io/docs/next/expect#tothrowerror
  test.todo(
    "severalReturnTypesFunction may trow some errors, use .toThrowError and .toThrow matcher and please assert text for severalReturnTypesFunction(3) and Exception Type for any other value severalReturnTypesFunction(n)"
  );
});
