import { render, screen } from "@testing-library/react";
import { Card } from "../../components/MiddleContent/Card";

describe("Testing like button from CardContainer", () => {
  it.todo("imports CardContainer and finds it in render");
  it.todo("checks that MDFavoriteBorder button clicks");
});
